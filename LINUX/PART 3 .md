- `curl ` -stands for client url.
- `curl -o (name) (hostname)` -(o) Stands for output <br>
  -it will download that host in that specific name.
- ` > ` it represents request  .
- ` < ` it represents response .

- `curl -I` -(I ) stands for (Include (HTTP info )/ head)
- `curl -v` -(v) stands for (verbose (detailed))
- `wget (hostname)` -stands for www get (easy to download.
- `python -m http.server(host number)` -to fetch a server. <br>
 we can use any other language we want.
- `127.0.0.1` it is a special type of if that always loop back to our home.
- `nano .bashrc` -nano is a text editor, ( . ) is hidden files , bashrc is bash run commands.
- `touch ` -creat new empty file.
- `echo (" ")`v -To show text in terminal.


  ## THAT WAS IT (BASIC LINUX) EXPLORE MORE BY YOURSELF!!
